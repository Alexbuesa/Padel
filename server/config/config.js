/**
 * ===================================
 * Puerto
 * ===================================
 * CREO que se pone así para que si en el entorno del servidor no hay puerto, significa que está en localhost, y se le pone 3000.
 */
process.env.PORT = process.env.PORT || 3000;